export interface IUsuario {
    id?: string,
    nombre: string,
    apellidos: string,
    idioma?: string,
    direccion?: string, 
    cp?: string, 
    poblacion?: string, 
    provincia?: string, 
    telefono: string, 
    email: string
}
